define(['jquery', 'three', 'happah'], function($, THREE, happah) {

     class Ray extends THREE.Ray {

               constructor() {
                    super();
                    //
               }

               intersect
          } // Class Sphere
     return {
          Sphere: Sphere
     }
});
